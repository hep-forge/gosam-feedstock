#! /usr/bin/bash
set -e

# TODO: GoSam is a Python + Fortran/C++ code generator, not CMake-based --
# replace this with its real build (likely `pip install .` plus a FORM/QGraf
# dependency check). This is a placeholder, not yet a working build.
echo "TODO: real GoSam build steps"
exit 1
